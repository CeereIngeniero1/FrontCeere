# Dashboard (Fase 5)

- `PersonalDashboardView` → `/app/dashboard`
- `TeamDashboardView` → `/app/equipo` (ADMIN / LEADER)
- `demoDashboard.ts` → agregador DEMO temporal
- API: `GET /dashboard/me`, `GET /dashboard/team`
