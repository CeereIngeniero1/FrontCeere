# Usado automáticamente por Vite en `npm run dev`.
VITE_API_URL=https://api-prueba.ceere.net/api

# TEMPORAL: demo hasta que BackCeere publique /auth/*.
# Cambiar a `api` cuando el backend de autenticación esté listo.
VITE_AUTH_MODE=demo
